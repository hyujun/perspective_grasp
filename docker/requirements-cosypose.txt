# CosyPose + MegaPose shared Docker runtime — pinned set for reproducibility.
# Consumed by docker/Dockerfile cosypose-runtime stage (shared between the
# `cosypose` and `megapose` compose services — happypose bundles both).
#
# NOTE: happypose itself is installed as a separate RUN in the Dockerfile
# with --no-deps because upstream pyproject extras (torchtnt / xarray /
# scikit-learn) destabilize the resolver. See memory/happypose_install_trap.md.

--extra-index-url https://download.pytorch.org/whl/cu126

# numpy 1.x ABI (cv_bridge constraint)
numpy<2

# PyTorch 2.6 + CUDA 12.6
torch==2.6.0
torchvision==0.21.0

# happypose runtime deps (the subset actually imported on the pose-estimation path)
trimesh==4.5.3
opencv-python-headless==4.10.0.84
scipy==1.14.1
scikit-image==0.24.0
imageio==2.36.0
omegaconf==2.3.0
pandas==2.2.3
transforms3d==0.4.2
panda3d==1.10.14
joblib==1.4.2
pybullet==3.2.6
bokeh==3.6.0
plyfile==1.1
pybind11==2.13.6
webdataset==0.2.100
pypng==0.20220715.0
addict==2.4.0
meshcat==0.3.2
roma==1.5.0
torchnet==0.0.4
seaborn==0.13.2

# pinocchio Python binding (SE(3) maths; cmeel-* helpers pulled transitively)
pin>=2.7
simplejson
pyyaml
tqdm
beautifulsoup4

# NOTE: pytorch3d is installed in a separate Dockerfile RUN with
# --no-build-isolation, because its setup.py does `import torch` at
# metadata-collection time — which fails in pip's default isolated
# build env even when torch is pinned in this same file. See the
# cosypose-runtime stage in docker/Dockerfile.
