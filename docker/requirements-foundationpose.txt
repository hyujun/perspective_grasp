# FoundationPose Docker runtime — pinned set for reproducibility.
# Consumed by docker/Dockerfile foundationpose-runtime stage.

--extra-index-url https://download.pytorch.org/whl/cu126
--find-links https://nvidia-kaolin.s3.us-east-2.amazonaws.com/torch-2.6.0_cu126.html

# numpy 1.x ABI (cv_bridge constraint)
numpy<2

# PyTorch 2.6 + CUDA 12.6
torch==2.6.0
torchvision==0.21.0

# Mesh + rendering utilities FoundationPose imports at runtime
trimesh==4.5.3
pyrender==0.1.45
open3d==0.19.0
opencv-python-headless==4.10.0.84
scipy==1.14.1
scikit-image==0.24.0
imageio==2.36.0
omegaconf==2.3.0
ruamel.yaml==0.18.6
joblib==1.4.2

# Differentiable rasterizer — SHA-pinned (was @v0.3.3)
nvdiffrast @ git+https://github.com/NVlabs/nvdiffrast.git@729261dc64c4241ea36efda84fbf532cc8b425b8

# NVIDIA kaolin (mesh + SDF helpers, torch ABI-bound).
# 0.18.0 is the first version with cp312 / torch-2.6 / cu126 wheels on
# the nvidia-kaolin S3 index (0.17.0 was cp311/earlier only).
kaolin==0.18.0
