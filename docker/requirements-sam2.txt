# SAM2 Docker runtime — pinned set for reproducibility.
# Consumed by docker/Dockerfile sam2-runtime stage.

--extra-index-url https://download.pytorch.org/whl/cu126

# numpy 1.x ABI (cv_bridge constraint)
numpy<2

# PyTorch 2.6 + CUDA 12.6
torch==2.6.0
torchvision==0.21.0

# Meta SAM2 — SHA-pinned (was @main; upstream last touched 2024-12).
# Pulls hydra-core + iopath as transitive deps.
SAM-2 @ git+https://github.com/facebookresearch/sam2.git@2b90b9f5ceec907a1c18123530e92e794ad901a4
