pose_graph_smoother:
  ros__parameters:
    # Used only when the upstream message has no header.frame_id.
    fallback_parent_frame: camera_color_optical_frame

    # GTSAM-gated parameters (declared only when the package is built with
    # GTSAM). Documented here for reference.
    # window_size: 20
    # prior_noise_pos: 0.01          # m
    # prior_noise_rot: 0.05          # rad
